<?xml version="1.0" encoding="UTF-8"?>
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:mailappor="http://schemas.microsoft.com/office/mailappversionoverrides/1.0"
  xsi:type="MailApp">

  <!--
    Synamail — Outlook add-in for Synaplan.

    DEV manifest (localhost:3000). The AppSource submission uses the versioned
    prod manifest (manifest_1-0-2_prod.xml), which carries the production GUID
    + hosted URLs.
    Keep both manifests in sync structurally; `make validate` checks both.
  -->

  <Id>5b6e8f74-2c5a-4f1d-9d3c-1b9a4a3c0e01</Id>
  <Version>1.0.0.6</Version>
  <ProviderName>Synaplan</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Synamail" />
  <Description DefaultValue="Your Synaplan workspace inside Outlook. Summarise, translate, draft replies, save emails to your knowledge base, and ask follow-ups — all without leaving Outlook." />
  <IconUrl DefaultValue="https://localhost:3000/assets/icon-64.png" />
  <HighResolutionIconUrl DefaultValue="https://localhost:3000/assets/icon-128.png" />
  <SupportUrl DefaultValue="https://web.synaplan.com/support" />

  <AppDomains>
    <AppDomain>https://web.synaplan.com</AppDomain>
    <AppDomain>https://addin.synaplan.com</AppDomain>
    <!-- Local dev loop: HTTPS-terminate the Synaplan Vite dev server on :5174
         (see docs/SYNAPLAN_INTEGRATION.md §7). Strip before AppSource. -->
    <AppDomain>https://localhost:5174</AppDomain>
  </AppDomains>

  <Hosts>
    <Host Name="Mailbox" />
  </Hosts>

  <Requirements>
    <Sets>
      <Set Name="Mailbox" MinVersion="1.8" />
    </Sets>
  </Requirements>

  <!-- FormSettings MUST declare a Form for every FormType listed in the Rule
       below. The Rule includes FormType="Edit", so an ItemEdit form is
       REQUIRED: Outlook on the web / new Outlook validate the base manifest
       server-side and silently drop the whole compose surface (reply/new mail
       ribbon button) when the ItemEdit form is missing — while classic
       Outlook, which builds its ribbon purely from VersionOverrides, still
       shows it. Keep both forms in sync with the prod manifest. -->
  <FormSettings>
    <Form xsi:type="ItemRead">
      <DesktopSettings>
        <SourceLocation DefaultValue="https://localhost:3000/src/taskpane/taskpane.html" />
        <RequestedHeight>450</RequestedHeight>
      </DesktopSettings>
    </Form>
    <Form xsi:type="ItemEdit">
      <DesktopSettings>
        <SourceLocation DefaultValue="https://localhost:3000/src/taskpane/taskpane.html" />
      </DesktopSettings>
    </Form>
  </FormSettings>

  <!-- ReadWriteItem is enough since v1 dropped all EWS (makeEwsRequestAsync)
       features: we read items, open reply/appointment forms, and use roaming
       settings. A narrower permission also eases AppSource review. -->
  <Permissions>ReadWriteItem</Permissions>

  <Rule xsi:type="RuleCollection" Mode="Or">
    <Rule xsi:type="ItemIs" ItemType="Message" FormType="Read" />
    <Rule xsi:type="ItemIs" ItemType="Message" FormType="Edit" />
  </Rule>

  <DisableEntityHighlighting>false</DisableEntityHighlighting>

  <VersionOverrides xmlns="http://schemas.microsoft.com/office/mailappversionoverrides" xsi:type="VersionOverridesV1_0">
    <Requirements>
      <bt:Sets DefaultMinVersion="1.8">
        <bt:Set Name="Mailbox" />
      </bt:Sets>
    </Requirements>

    <Hosts>
      <Host xsi:type="MailHost">
        <DesktopFormFactor>
          <FunctionFile resid="Commands.Url" />

          <ExtensionPoint xsi:type="MessageReadCommandSurface">
            <OfficeTab id="TabDefault">
              <Group id="msgReadGroup">
                <Label resid="GroupLabel" />
                <Control xsi:type="Button" id="msgReadOpenPaneButton">
                  <Label resid="TaskpaneButton.Label" />
                  <Supertip>
                    <Title resid="TaskpaneButton.Label" />
                    <Description resid="TaskpaneButton.Tooltip" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16x16" />
                    <bt:Image size="32" resid="Icon.32x32" />
                    <bt:Image size="80" resid="Icon.80x80" />
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <SourceLocation resid="Taskpane.Url" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>

          <ExtensionPoint xsi:type="MessageComposeCommandSurface">
            <OfficeTab id="TabDefault">
              <Group id="msgComposeGroup">
                <Label resid="GroupLabel" />
                <Control xsi:type="Button" id="msgComposeOpenPaneButton">
                  <Label resid="TaskpaneButton.Label" />
                  <Supertip>
                    <Title resid="TaskpaneButton.Label" />
                    <Description resid="TaskpaneButton.Tooltip" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16x16" />
                    <bt:Image size="32" resid="Icon.32x32" />
                    <bt:Image size="80" resid="Icon.80x80" />
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <SourceLocation resid="Taskpane.Url" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>

    <Resources>
      <bt:Images>
        <bt:Image id="Icon.16x16" DefaultValue="https://localhost:3000/assets/icon-16.png" />
        <bt:Image id="Icon.32x32" DefaultValue="https://localhost:3000/assets/icon-32.png" />
        <bt:Image id="Icon.80x80" DefaultValue="https://localhost:3000/assets/icon-80.png" />
      </bt:Images>
      <bt:Urls>
        <bt:Url id="Commands.Url" DefaultValue="https://localhost:3000/src/commands/commands.html" />
        <bt:Url id="Taskpane.Url" DefaultValue="https://localhost:3000/src/taskpane/taskpane.html" />
      </bt:Urls>
      <bt:ShortStrings>
        <bt:String id="GroupLabel" DefaultValue="Synamail" />
        <bt:String id="TaskpaneButton.Label" DefaultValue="Open Synamail" />
      </bt:ShortStrings>
      <bt:LongStrings>
        <bt:String id="TaskpaneButton.Tooltip" DefaultValue="Open the Synamail assistant to summarise, translate, draft replies, save emails to your knowledge base, or ask follow-up questions." />
      </bt:LongStrings>
    </Resources>

    <!-- NOTE: no nested VersionOverridesV1_1 block. We previously nested one
         to add SupportsPinning, but that structure makes the ribbon button
         silently dead on M365 business accounts in Outlook on the web / new
         Outlook (no network request, no error) — see
         https://github.com/OfficeDev/office-js/issues/5902. A working task
         pane beats a pinnable one, so pinning is dropped until the platform
         bug is fixed. Keep this in sync with the prod manifest. -->
  </VersionOverrides>
</OfficeApp>
