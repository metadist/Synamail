# Dependencies
node_modules/

# Build output
dist/
dist-*/
coverage/
.vite/

# Tests
playwright-report/
test-results/
*.lcov

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Env / secrets
.env
.env.local
.env.*.local

# Internal/private ops note — real SSH/host/deploy details (NEVER commit).
.development

# Local share artifacts (sideload ZIP handed to testers).
*.zip

# Office add-in dev certs (auto-generated)
.office-addin-dev-certs/

# Local dev-loop logs (start-dev.sh)
.dev-logs/

# Generated artefacts (committed only at release time)
manifest.unified.json

# Generated Zod schemas (committed once Sprint 3 wires them up)
src/generated/openapi-spec.json
