/**
 * Function-file commands for ribbon buttons that don't open the taskpane.
 *
 * v0.1: nothing here. All ribbon buttons use `ShowTaskpane`. Phase-2
 * event-based handlers (`OnNewMessageCompose`, `OnMessageSend` Smart
 * Alerts) will register their handlers via `Office.actions.associate(...)`.
 */

Office.onReady(() => {
  // Intentionally empty for v0.1.
})
